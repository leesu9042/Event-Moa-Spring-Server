spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/event_moa
    username: root
    password: 
  jpa:
    hibernate:
      ddl-auto: update
    properties.hibernate:

      format_sql: true
      show_sql: true
      jdbc:
        batch_size: 200
    open-in-view: false

  jwt:
    jwt-secret: asdfasdfasdfasdfasdf
    header: Authorization
    prefix: Bearer
    accessExp: 3600
    refreshExp: 2960000

